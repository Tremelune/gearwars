# gearwars
A shitty gearing calculator

# setup
Install Node.js and NPM (`brew install node`), then:

`$ npm install`

# run tests
`$ npm test`

# run local
`$ npm start`

# production release
`$ npm run deploy`
